import { DataConverterPipe } from './data-converter.pipe';

describe('DataConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new DataConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
